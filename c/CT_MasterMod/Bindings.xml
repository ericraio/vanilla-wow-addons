<Bindings>
	<Binding name="CT_CPTOGGLE" header="CT_CPBINDS" runOnUp="true">
		if ( keystate == "down" ) then
			if ( CT_CPFrame:IsVisible() ) then
				HideUIPanel(CT_CPFrame);
			else
				ShowUIPanel(CT_CPFrame);
			end
		end
	</Binding>
	<Binding name="CT_TARGETLASTTARGET" runOnUp="true" header="CT_TARGETLAST">
		if ( keystate == "up" ) then
			TargetLastTarget();
		end
	</Binding>
</Bindings>