<Bindings>
	<Binding name="Hold For ZoomMap" description="Hold to expand the Minimap" runOnUp="true" header="Zoom Map">
		if ( keystate == "down" ) then
			ToggleZoomMap(1);
		else
			ToggleZoomMap(0);
		end
	</Binding>
</Bindings>