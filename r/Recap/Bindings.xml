<Bindings>
	<Binding name="Toggle Recap" header="RECAP">
		if RecapFrame:IsShown() then
			RecapFrame_Hide()
		else
			RecapFrame_Show()
		end
	</Binding>
	<Binding name="Toggle Last/All Fights">
		Recap_ToggleView()
	</Binding>
	<Binding name="Options">
		if RecapOptFrame:IsShown() then
			RecapOptFrame:Hide()
		else
			RecapOptFrame:Show()
		end
	</Binding>
</Bindings>
