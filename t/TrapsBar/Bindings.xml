<Bindings>
	<!-- Bindings.xml  file for TrapsBar -->
	<!-- Show me my config ... Sweetness! -->
  	<Binding name="Load TrapsBar Config" header="TRAPSBAR">
  		TrapsBarConfig:Show();
  	</Binding>
	<!-- end -->
	<!-- Hide/Unhide the Bar -->
  	<Binding name="Hide/Unhide TrapsBar">
     local frame = getglobal("TrapsBar");
     if (frame) then
       if(  frame:IsVisible() ) then
         frame:Hide();
       else
         frame:Show();
       end
     end
  	</Binding>
	<!-- end -->
