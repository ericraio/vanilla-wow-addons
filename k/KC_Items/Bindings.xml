<Bindings>
	<Binding name="KC_Items_LinkView" header="KC_Items">
        if (KC_Items.linkview) then
			if (KC_LinkviewFrame:IsVisible()) then
				KC_Items.linkview.gui:Hide()
			else
				KC_Items.linkview.gui:Show();    
			end	
        end
	</Binding>
</Bindings>
