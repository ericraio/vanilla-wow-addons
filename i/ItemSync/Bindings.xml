<Bindings>
	<Binding name="ItemsSync" runOnUp="true" header="ITEMSYNC_HEADER">
		if ( keystate == "up" ) then
			if (ISync) then ISync:MainFrame_Binding(); end
		end
	</Binding>
	<Binding name="BagView" runOnUp="true">
		if ( keystate == "up" ) then
			if (ISync) then ISync:BV_Binding(); end
		end
	</Binding>
	<Binding name="ISync ItemID" runOnUp="true">
		if ( keystate == "up" ) then
			if (ISync) then ISync:ItemIDSearch_Binding(); end
		end
	</Binding>
	<Binding name="Favorites" runOnUp="true">
		if ( keystate == "up" ) then
			if (ISync) then ISync:FavFrame_Binding(); end
		end
	</Binding>
</Bindings>
