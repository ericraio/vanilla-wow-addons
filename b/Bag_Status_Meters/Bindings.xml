<Bindings>
	<Binding name="BSM_OPTIONS" runOnUp="true" header="BSM">
		if ( keystate == "down" ) then
            if not getglobal("BSM_Options"):IsVisible() then
                getglobal("BSM_Options"):Show();
            else
                getglobal("BSM_Options"):Hide();
            end
		end
	</Binding>
</Bindings>
